# OneRoster API port to serve on:
PORT=3000

# Database type: 'postgres' or 'mssql'
DB_TYPE=postgres

# Ed-Fi ODS Postgres database settings:
DB_HOST=localhost
DB_USER=postgres
DB_PASS=P@ssw0rd
DB_NAME=EdFi_Ods_Populated_Template

# Ed-Fi ODS MSSQL database settings:
# (Use these when DB_TYPE=mssql)
MSSQL_SERVER=localhost
MSSQL_DATABASE=EdFi_Ods_Populated_Template
MSSQL_USER=
MSSQL_PASSWORD=
MSSQL_ENCRYPT=true
MSSQL_TRUST_SERVER_CERTIFICATE=true

# set to `prod` or `` (empty) for production
NODE_ENV=dev

# pg-boss settings (PostgreSQL only):
PGBOSS_CRON=*/15 * * * *

# OAuth2 settings - these must be filled out!
# (except for local testing - if blank, there's no auth, all requests will succeed)
OAUTH2_ISSUERBASEURL=https://dev-5n8uf6ov.us.auth0.com/
OAUTH2_AUDIENCE=http://localhost:3000
OAUTH2_TOKENSIGNINGALG=RS256
